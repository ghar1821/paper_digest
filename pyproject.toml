[project]
name = "paper-digest"
version = "0.1.0"
description = "Convert arXiv/local PDFs to Markdown with image extraction"
requires-python = ">=3.12"
dependencies = [
    "marker-pdf>=1.10.2",
    "ollama>=0.6.1",
    "requests>=2.33.1",
]

[project.scripts]
paper-digest = "convert:main"
